/**
 * global configuration for layout, in px
 *
 * todo: replace by MantineThemeOverride, to let me do syntax: ${(props) => props.theme.breakpoint.sm}
 */
export const breakpoints = {
    tablet: '770px',
    desktop: '400px',
}
